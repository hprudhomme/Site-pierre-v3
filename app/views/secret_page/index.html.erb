<div class="access-admin-page">
    <%= form_tag(secret_page_index_path, :method => "get", id: "admin-form") do %>
        <%= text_field_tag :admin, params[:admin], id: 'input-admin-form', placeholder: "Mot de passe" %>
        <button type="submit" class="submit-admin-form btn mt-3">Submit</button>
    <% end %>
</div>